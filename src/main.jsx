import React from 'react';
import ReactDOM from 'react-dom/client';
import TonyAssistant from './TonyAssistant.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TonyAssistant />
  </React.StrictMode>
);
